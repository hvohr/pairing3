var numbers = [8, 12, 23, 36, 49, 62, 80, 97]
for (var i = 0; i < numbers.length; i++) {
    if (numbers[i] > 30) {
        console.log(numbers[i]);
    }
};

for (var i = 0; i < numbers.length; i++) {
    if (numbers[i] < 60) {
        console.log(numbers[i]);
    }
};